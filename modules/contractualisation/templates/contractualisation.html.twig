{#
/**
 * @file contractualisation.html.twig
 * Default theme implementation to present Contractualisation data.
 *
 * This template is used when viewing Contractualisation pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_contractualisation()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('contractualisation') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
